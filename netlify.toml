[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

# React Router SPA — 새로고침 시 404 방지 (필수)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
